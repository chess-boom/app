<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:ChessBoom.ViewModels"
             x:Class="ChessBoom.Views.AtomicView"
             x:DataType="viewModels:AtomicViewModel">
    <Design.DataContext>
        <!-- ReSharper disable once Xaml.ConstructorWarning -->
        <viewModels:AtomicViewModel />
    </Design.DataContext>
    <StackPanel HorizontalAlignment="Center">
        <Label Content="Atomic Board" FontSize="50" HorizontalAlignment="Center" />
        <Grid Name="ChessBoard" HorizontalAlignment="Center" PointerPressed="ChessBoard_MouseLeftButtonDown" Focusable="True"/>
    </StackPanel>
</UserControl>